<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="KeyboardAnalysis.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KeyboardAnalysis"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Keyboard Analysis">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Keyboard Analysis" Style="{StaticResource TitleTextBlockStyle}" />
        <TextBlock Grid.Row="1" Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />
        <TextBlock Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center"
                   Text="Heatmaps, statistics, trends, and insights will appear here." Opacity="0.65" />
    </Grid>
</Window>
